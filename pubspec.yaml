name: flutter_mmcalendar
description: Myanmar calendar library for flutter application with many utility methods and calculations.
version: 1.1.1
homepage: https://github.com/mixin27/flutter-mmcalendar
issue_tracker: https://github.com/mixin27/flutter-mmcalendar/issues
funding:
  - https://www.buymeacoffee.com/kyawzayartun98
screenshots:
  - description: "This screenshot shows a sample formatted date string by supported languages"
    path: example.PNG

environment:
  sdk: ">=3.1.2 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:

# This package supports all platforms listed below.
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

topics:
  - calendar
  - myanmar
  - myanmar-calendar
  - utility
  - datetime
