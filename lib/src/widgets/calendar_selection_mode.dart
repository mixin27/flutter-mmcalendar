/// Selection modes for the Myanmar calendar
enum CalendarSelectionMode {
  /// Select a single date
  single,

  /// Select a range of dates (start and end)
  range,

  /// Select multiple individual dates
  multi,
}
